// Dean Black - nameAge.js

var name = String(prompt("What is your name? ")); // Asks for name
var age = String(prompt("How old are you? ")); // Asks for age

alert("Your name is " + name + " and you are "+ age+ " years old." ); // Outputs data in sentence
